# API Configuration
# Use empty string to let axios use relative paths with /api prefix (proxied by Vite)
# OR use http://localhost:8000 to bypass proxy (may have CORS issues)
VITE_API_BASE_URL=

# App Configuration
VITE_APP_NAME=AI Vision Platform
VITE_APP_VERSION=1.0.0
